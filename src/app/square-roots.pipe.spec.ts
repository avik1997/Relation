import { SquareRootsPipe } from './square-roots.pipe';

describe('SquareRootsPipe', () => {
  it('create an instance', () => {
    const pipe = new SquareRootsPipe();
    expect(pipe).toBeTruthy();
  });
});
